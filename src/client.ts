/**
 * Client-side integration with external services (if any).
 *
 * TODO(skeleton): fill in any external API / process communication
 * pi-guard needs, e.g. probing the environment the agent runs in.
 */
