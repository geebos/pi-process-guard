{
  "name": "pi-process-guard",
  "version": "2.0.0",
  "description": "pi-guard — guard extension for pi coding agent (initial skeleton)",
  "type": "module",
  "main": "./extensions/index.ts",
  "bin": {
    "pi-guard": "./dist/bin/pi-guard.js",
    "pi-guard-janitor": "./dist/src/janitor/index.js"
  },
  "files": [
    "bin/",
    "extensions/",
    "src/",
    "dist/",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "test": "node --test --test-force-exit \"test/unit/*.test.ts\" \"test/integration/*.test.ts\"",
    "test:unit": "node --test --test-force-exit \"test/unit/*.test.ts\"",
    "test:integration": "node --test --test-force-exit \"test/integration/*.test.ts\"",
    "check": "npm run typecheck && npm test",
    "publish:local": "npm version patch --no-git-tag-version && npm publish --registry http://localhost:9007"
  },
  "keywords": [
    "pi-package",
    "pi",
    "pi-extension",
    "pi-coding-agent",
    "guard"
  ],
  "author": "geebos",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/geebos/pi-process-guard.git"
  },
  "bugs": {
    "url": "https://github.com/geebos/pi-process-guard/issues"
  },
  "homepage": "https://github.com/geebos/pi-process-guard#readme",
  "engines": {
    "node": ">=22.18"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.83.0",
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-tui": "0.83.0",
    "@types/node": "22.20.1",
    "typebox": "1.3.8",
    "typescript": "5.9.3"
  }
}
